# Handoff · Wave 1 · 2026-07-18 (W1-01 pilot)

## TL;DR
1. Dual-rack instrument row is taller, equal-height, and top-anchored like a Live workbench (not a centered marketing card).
2. Shell widened (`max-w-7xl`) with flatter device shadow; signal rails wire INPUT → MONITOR.
3. 4|8 column split preserved for smoke; visual family closer to refs 01/06.

## Implemented
- **W1-01** · Dual-rack INPUT|MONITOR proportions / equal height / workbench shell  
  Files: `apps/ui/src/App.tsx`, `apps/ui/src/index.css`

## Verify
| Gate | Result |
| --- | --- |
| lint (`tsc`) | pass |
| lint:style | pass |
| test:unit | pass (892) |
| build | pass |
| smoke (responsive-layout + theme-shell) | pass (6) |
| full `npm run verify` | partial (targeted smoke only this block) |

## Side-by-side
- Current: `apps/ui/design/compare/20260718-wave1-w101/dual-rack-idle.png`
- Current full shell: `apps/ui/design/compare/20260718-wave1-w101/shell-idle-desktop.png`
- Reference: `apps/ui/design/reference/06-signal-monitor.png` (loaded-file state) + `01-input-monitor.png`
- Note: compare is **idle** vs ref **loaded+playing** — proportions/chrome family only, not identical state.

## Residual top 10 (open)
1. **W1-02** — Full-width orange RUN ANALYSIS CTA (same surface)
2. **W1-03** — Monitor idle AWAITING SIGNAL instrument chrome (replace marketing idle)
3. **W1-04** — Active multi-band spectrum legend chrome
4. **W1-05** — Terminal diagnostics density
5. **W1-06** — IdleValueProp utilitarian rewrite (still marketing-leaning in capture)
6. **W1-07** — Title-strip LED polish on shell racks
7. **W0-01** — Owner baseline branch/commit (still pending)
8. **W2-01** — MetricTile unify on measurement dashboard
9. **W2-06** — Kill uncalibrated score heroes
10. **W2-07** — Debris detector test

## Need from owner
- `continue` (default → W1-02 or W1-03 next)  
- `pivot to <ID>`  
- `freeze`  
- `ship` / commit this pilot  

**Pilot judgment:** handoff is scannable with screenshots + residual list. Safe to authorize more Wave 1 blocks if you like the shell direction. Multi-wave Ralph still optional.
